{
  "mcpServers": {
    "stripe": {
      "command": "cmd",
      "args": [
        "/c",
        "npx",
        "-y",
        "@stripe/mcp@latest"
      ],
      "env": {
        "STRIPE_SECRET_KEY": "<your-stripe-secret-key>"
      }
    }
  }
}